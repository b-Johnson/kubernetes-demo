apiVersion: v1
kind: Service
metadata:
  name: nginx-frontend
  namespace: demo
  labels:
    app: nginx-frontend
    service: nginx-frontend
spec:
  ports:
  - port: 80
    targetPort: 80
    name: http
  selector:
    app: nginx-frontend
