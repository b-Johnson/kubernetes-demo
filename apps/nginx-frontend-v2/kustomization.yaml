apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
- service.yaml
- configmap.yaml

commonLabels:
  app.kubernetes.io/name: nginx-frontend-v2
  app.kubernetes.io/instance: nginx-frontend-v2
  app.kubernetes.io/component: frontend
  app.kubernetes.io/part-of: nginx-demo
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/version: v2

images:
- name: nginx
  newTag: 1.25-alpine
