apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nginx-frontend-v2
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    app.kubernetes.io/name: nginx-frontend-v2
    app.kubernetes.io/part-of: nginx-demo
spec:
  project: default
  source:
    repoURL: https://github.com/b-Johnson/kubernetes-demo
    targetRevision: HEAD
    path: apps/nginx-frontend-v2
  destination:
    server: https://kubernetes.default.svc
    namespace: demo
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
    - CreateNamespace=true
    - PrunePropagationPolicy=foreground
    - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  revisionHistoryLimit: 3
